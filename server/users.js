const users = [];
const addUser = ({ id, tag, room }) => {
  tag = tag.trim().toUpperCase();
  room = tag.trim().toUpperCase();

  const existingUser = users.find();
};

const removeUser = () => {};

const getUser = () => {};

const getUsersInRoom = () => {};
